apiVersion: v1
kind: Service
metadata:
  name: service-roboshop
spec:
  selector:
    environment: development
    project: roboshop
  ports:
  - protocol: TCP
    port: 80 # service port
    targetPort: 80   #container port
