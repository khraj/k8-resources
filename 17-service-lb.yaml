apiVersion: v1
kind: Service
metadata:
  name: service-lb
spec:
  type: LoadBalancer
  selector:
    environment: development
    project: roboshop
    purpose: loadbalancer-service
  ports:
  - protocol: TCP
    port: 80 
    targetPort: 80