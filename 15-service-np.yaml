apiVersion: v1
kind: Service
metadata:
  name: np-service-demo
spec:
  type: NodePort
  selectors:
    environment: development
    project: roboshop
    purpose: demo
  ports:
  - protocol: tcp
    port: 80 
    targetPort: 80