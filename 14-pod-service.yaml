apiVersion: v1
kind: Pod
metadata:
  name: pod-service-demo
  #these lables are used to register with service
  labels:
    environment: development
    project: roboshop 
spec:
  containers:
  - name: nginx
    image: nginx